package ValAndVar
//VAL stand for value, and it is immutable.Val cannot be change
//VAR stand for variable, and it is mutable
fun main(){
    //The kotlin uses type inference to deduce the variable type from the identifier.When the code is compiled the Kotlin compiler will infer the type .
    //If the variable is initialized at the declaration time
    val bob="Bobo"
    //bob cannot be reassigned

}

